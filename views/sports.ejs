<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/styles.css" rel="stylesheet">
    <title>
        <%= title %>
    </title>
</head>
<body class="bg-green-100">
    <div class="grid grid-cols-4">
        <div class="col-start-2 col-span-2">
            <%- include("header.ejs") %>
            
                <p class="text-xl flex items-center">Welcome <%= playerName %></p>
            <div>
                <a href="/signout" class="hover:underline">Signout</a>
            </div>
            <% if (userRole === 'admin'){ %>
            <div>
                <a href="/createSport" class="hover:underline"> Create a New Sport</a>
            </div>
            <div>
                <a href="#" class="hover:underline">View Reports</a>
            </div>
            <% } %>
            <% if (userRole === 'admin'){ %>
            <div>
                <div>
                    <h6 class="text-red-600">
                        <%= (allSports.length==0)?"No Sports sessions are created yet":"" %>
                    </h6>
                </div>
                <ul class="list-none">
                    <% for (let i=0; i<allSports.length; i++) {%>
                        <li>
                            <div class="text-gray-600">
                                <h3><a href="/sports/<%=allSports[i].id %>" class="hover:underline"><%= i+1 %>. <%= allSports[i].name %></a></h3>
                            </div>
                        </li>
                    <% } %>
                </ul>
            </div>
            <% } else { %>
            <div>
                <div>
                    <h6 class="text-red-600">
                        <%= (playerSports.length==0)?"No Sports sessions are created yet":"" %>
                    </h6>
                </div>
                <ul class="list-none">
                    <% for (let i=0; i<playerSports.length; i++) {%>
                        <li>
                            <div class="text-gray-600">
                                <h3><a href="/sports/<%=playerSports[i].id %>" class="hover:underline"><%= i+1 %>. <%= playerSports[i].name %></a></h3>
                            </div>
                        </li>
                    <% } %>
                </ul>
            </div>
            <% } %>
        </div> 
    </div>
</body>
</html>